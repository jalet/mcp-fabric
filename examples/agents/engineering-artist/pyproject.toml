[project]
name = "engineering-artist-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "strands-agents",
    "strands-agents-tools",
    "flask",
    "boto3",
    "gunicorn",
    "pyyaml",
]
