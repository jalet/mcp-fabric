[project]
name = "agent-libs"
version = "0.1.0"
description = "Shared libraries for MCP Fabric agents"
requires-python = ">=3.12"
dependencies = [
    "gunicorn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
