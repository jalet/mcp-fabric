apiVersion: v1
kind: Service
metadata:
  name: mcp-fabric-operator-metrics
  namespace: mcp-fabric-system
  labels:
    app.kubernetes.io/name: mcp-fabric-operator
    app.kubernetes.io/component: operator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-fabric-operator
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
