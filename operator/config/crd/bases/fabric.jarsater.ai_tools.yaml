---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: tools.fabric.jarsater.ai
spec:
  group: fabric.jarsater.ai
  names:
    kind: Tool
    listKind: ToolList
    plural: tools
    shortNames:
    - tl
    singular: tool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Tool ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Number of tools
      jsonPath: .status.availableTools
      name: Tools
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tool declares a Strands tool bundle containing @tool decorated
          functions.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ToolSpec defines the desired state of Tool.
            properties:
              entryModule:
                description: EntryModule is the Python module path to import (e.g.,
                  "mypackage.tools").
                type: string
              image:
                description: |-
                  Image is the OCI image containing the tool package code.
                  The image should contain Python code with @tool decorated functions.
                minLength: 1
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: ImagePullPolicy determines when to pull the image.
                enum:
                - Always
                - IfNotPresent
                - Never
                type: string
              imagePullSecrets:
                description: ImagePullSecrets are references to secrets for pulling
                  the image.
                items:
                  description: LocalObjectReference references a local object by name.
                  properties:
                    name:
                      description: Name of the referenced object.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              tools:
                description: |-
                  Tools declares the available tools in this package.
                  If empty, tools will be discovered at runtime or via introspection.
                items:
                  description: ToolDefinition defines a single tool within a Tool
                    resource.
                  properties:
                    description:
                      description: Description explains what the tool does.
                      type: string
                    inputSchema:
                      description: InputSchema is the JSON Schema for tool input parameters.
                      properties:
                        description:
                          description: Description of this schema element.
                          type: string
                        items:
                          description: Items defines array item schema as raw JSON
                            (when type=array).
                          x-kubernetes-preserve-unknown-fields: true
                        properties:
                          description: Properties defines object properties as raw
                            JSON (when type=object).
                          x-kubernetes-preserve-unknown-fields: true
                        required:
                          description: Required lists required property names.
                          items:
                            type: string
                          type: array
                        type:
                          description: Type is the JSON Schema type (object, string,
                            number, etc.).
                          enum:
                          - object
                          - string
                          - number
                          - integer
                          - boolean
                          - array
                          type: string
                      type: object
                    name:
                      description: Name is the tool function name (matches @tool decorated
                        function).
                      minLength: 1
                      type: string
                    outputSchema:
                      description: OutputSchema is the JSON Schema for tool output.
                      properties:
                        description:
                          description: Description of this schema element.
                          type: string
                        items:
                          description: Items defines array item schema as raw JSON
                            (when type=array).
                          x-kubernetes-preserve-unknown-fields: true
                        properties:
                          description: Properties defines object properties as raw
                            JSON (when type=object).
                          x-kubernetes-preserve-unknown-fields: true
                        required:
                          description: Required lists required property names.
                          items:
                            type: string
                          type: array
                        type:
                          description: Type is the JSON Schema type (object, string,
                            number, etc.).
                          enum:
                          - object
                          - string
                          - number
                          - integer
                          - boolean
                          - array
                          type: string
                      type: object
                  required:
                  - name
                  type: object
                type: array
            required:
            - image
            type: object
          status:
            description: ToolStatus defines the observed state of Tool.
            properties:
              availableTools:
                description: AvailableTools lists tools discovered or declared in
                  the package.
                items:
                  description: ToolDefinition defines a single tool within a Tool
                    resource.
                  properties:
                    description:
                      description: Description explains what the tool does.
                      type: string
                    inputSchema:
                      description: InputSchema is the JSON Schema for tool input parameters.
                      properties:
                        description:
                          description: Description of this schema element.
                          type: string
                        items:
                          description: Items defines array item schema as raw JSON
                            (when type=array).
                          x-kubernetes-preserve-unknown-fields: true
                        properties:
                          description: Properties defines object properties as raw
                            JSON (when type=object).
                          x-kubernetes-preserve-unknown-fields: true
                        required:
                          description: Required lists required property names.
                          items:
                            type: string
                          type: array
                        type:
                          description: Type is the JSON Schema type (object, string,
                            number, etc.).
                          enum:
                          - object
                          - string
                          - number
                          - integer
                          - boolean
                          - array
                          type: string
                      type: object
                    name:
                      description: Name is the tool function name (matches @tool decorated
                        function).
                      minLength: 1
                      type: string
                    outputSchema:
                      description: OutputSchema is the JSON Schema for tool output.
                      properties:
                        description:
                          description: Description of this schema element.
                          type: string
                        items:
                          description: Items defines array item schema as raw JSON
                            (when type=array).
                          x-kubernetes-preserve-unknown-fields: true
                        properties:
                          description: Properties defines object properties as raw
                            JSON (when type=object).
                          x-kubernetes-preserve-unknown-fields: true
                        required:
                          description: Required lists required property names.
                          items:
                            type: string
                          type: array
                        type:
                          description: Type is the JSON Schema type (object, string,
                            number, etc.).
                          enum:
                          - object
                          - string
                          - number
                          - integer
                          - boolean
                          - array
                          type: string
                      type: object
                  required:
                  - name
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last observed generation.
                format: int64
                type: integer
              ready:
                description: Ready indicates the Tool is validated and available.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
