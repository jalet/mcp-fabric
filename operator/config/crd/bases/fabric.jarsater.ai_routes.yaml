---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: routes.fabric.jarsater.ai
spec:
  group: fabric.jarsater.ai
  names:
    kind: Route
    listKind: RouteList
    plural: routes
    shortNames:
    - rt
    singular: route
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: All backends ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Active rules
      jsonPath: .status.activeRules
      name: Rules
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Route declares routing rules from the gateway to agents.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RouteSpec defines the desired state of Route.
            properties:
              defaults:
                description: Defaults configure fallback behavior.
                properties:
                  backend:
                    description: Backend is the fallback agent when no rules match.
                    properties:
                      agentRef:
                        description: AgentRef references an Agent by name.
                        properties:
                          name:
                            description: Name of the Agent.
                            type: string
                          namespace:
                            description: Namespace of the Agent (defaults to route
                              namespace).
                            type: string
                        required:
                        - name
                        type: object
                      weight:
                        default: 100
                        description: Weight determines selection probability (0-100).
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                    required:
                    - agentRef
                    type: object
                  circuitBreaker:
                    description: CircuitBreaker configures request limiting.
                    properties:
                      maxConcurrent:
                        default: 100
                        description: MaxConcurrent limits concurrent requests to backends.
                        format: int32
                        minimum: 1
                        type: integer
                      maxQueueSize:
                        default: 50
                        description: MaxQueueSize limits queued requests when at capacity.
                        format: int32
                        minimum: 0
                        type: integer
                      queueTimeout:
                        default: 30s
                        description: QueueTimeout is how long requests wait in queue.
                        type: string
                      requestTimeout:
                        default: 5m
                        description: RequestTimeout is the maximum backend request
                          duration.
                        type: string
                    type: object
                  rejectUnmatched:
                    default: false
                    description: |-
                      RejectUnmatched returns an error for unmatched requests.
                      If false and no default backend, returns 404.
                    type: boolean
                type: object
              gatewaySelector:
                additionalProperties:
                  type: string
                description: GatewaySelector identifies which gateway consumes these
                  routes.
                type: object
              rules:
                description: Rules define routing conditions and backends.
                items:
                  description: RouteRule defines a single routing rule.
                  properties:
                    backends:
                      description: Backends are the target agents (supports weighted
                        routing).
                      items:
                        description: RouteBackend defines a target agent for routing.
                        properties:
                          agentRef:
                            description: AgentRef references an Agent by name.
                            properties:
                              name:
                                description: Name of the Agent.
                                type: string
                              namespace:
                                description: Namespace of the Agent (defaults to route
                                  namespace).
                                type: string
                            required:
                            - name
                            type: object
                          weight:
                            default: 100
                            description: Weight determines selection probability (0-100).
                            format: int32
                            maximum: 100
                            minimum: 0
                            type: integer
                        required:
                        - agentRef
                        type: object
                      minItems: 1
                      type: array
                    match:
                      description: Match defines conditions for this rule.
                      properties:
                        agent:
                          description: Agent matches requests with explicit agent
                            name.
                          type: string
                        headers:
                          additionalProperties:
                            type: string
                          description: Headers matches request metadata headers.
                          type: object
                        intentRegex:
                          description: |-
                            IntentRegex matches the request intent field.
                            Uses RE2 regex syntax.
                          type: string
                        tenantId:
                          description: TenantID matches requests from a specific tenant.
                          type: string
                      type: object
                    name:
                      description: Name is a unique identifier for this rule.
                      minLength: 1
                      type: string
                    priority:
                      default: 0
                      description: Priority determines rule evaluation order (higher
                        = first).
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - backends
                  - match
                  - name
                  type: object
                type: array
            type: object
          status:
            description: RouteStatus defines the observed state of Route.
            properties:
              activeRules:
                description: ActiveRules is the count of compiled rules.
                format: int32
                type: integer
              backends:
                description: Backends lists the health of all referenced agents.
                items:
                  description: BackendStatus represents the health of a backend agent.
                  properties:
                    agentRef:
                      description: AgentRef identifies the agent.
                      properties:
                        name:
                          description: Name of the Agent.
                          type: string
                        namespace:
                          description: Namespace of the Agent (defaults to route namespace).
                          type: string
                      required:
                      - name
                      type: object
                    endpoint:
                      description: Endpoint is the resolved agent service URL.
                      type: string
                    ready:
                      description: Ready indicates the agent is available.
                      type: boolean
                  required:
                  - agentRef
                  - ready
                  type: object
                type: array
              compiledConfigMap:
                description: CompiledConfigMap is the name of the generated routes
                  ConfigMap.
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last observed generation.
                format: int64
                type: integer
              ready:
                description: Ready indicates all referenced agents are available.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
